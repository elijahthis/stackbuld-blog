/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
			"loremflickr.com",
			"cloudflare-ipfs.com",
			"ui-avatars.com",
			"res.cloudinary.com",
		],
	},
};

module.exports = nextConfig;
